export declare type AsyncQueue<T> = {
    run(handler: () => Promise<T>): Promise<T>;
    stop(): void;
};
export declare function createQueue<T>(callsPerSec: number): AsyncQueue<T>;
export declare function delay(ms: number): Promise<unknown>;
